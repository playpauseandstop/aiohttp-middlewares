machine:
  python:
    version: 3.6.1

dependencies:
  cache_directories:
    - env/

  override:
    - pip install -U bootstrapper pip wheel
    - make
    - make setup-pyenv

test:
  override:
    - make test

deployment:
  release:
    tag: /v\d+.\d+.\d+.*/
    commands:
      - make deploy
